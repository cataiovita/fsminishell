# use make to compile
make
make run
# first command need to be "create fs" to create the file system;
create fs
# try out the classic linux file system cmds: ls, touch, cd, cd .., mkdir, rmdir, tree, pwd, find;
# last command need to be "delete fs" to delete the created file system and stop execution;
delete fs
